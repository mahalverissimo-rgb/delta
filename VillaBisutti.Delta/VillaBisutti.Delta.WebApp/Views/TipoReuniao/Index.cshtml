@model IEnumerable<VillaBisutti.Delta.Core.Model.TipoReuniao>

@{
	Layout = "~/Views/Shared/_ConfiguracoesLayout.cshtml";
	//ViewBag.Title = "Index";
}

@section scripts {

    <script language="javascript" type="text/javascript">
        $(document).ready(function () {
            $(".PopUpActionLinks").click(function (e) {
                e.preventDefault();
                ShowPopUp($(this).attr("href"), $(this).attr("title"), 790);
            });
        });
    </script>
}
@if (ViewBag.IsBlocked == "TRUE")
{
	<script language="javascript" type="text/javascript">
		$(document).ready(function () {
			LockPage();
		});
	</script>
}
@Html.Action("Create", "TipoReuniao")
<table class="table">
	<tr>
		<th>
			@Html.DisplayNameFor(model => model.Nome)
		</th>
		<th>
			@Html.DisplayNameFor(model => model.DiasAntesEvento)
		</th>
		<th>
			@Html.DisplayNameFor(model => model.HorarioDuracao)
		</th>
		<th>
			Dias disponíveis:
		</th>
		<th></th>
	</tr>

	@foreach (var item in Model)
	{
		<tr>
			<td>
				@Html.DisplayFor(modelItem => item.Nome)
			</td>
			<td>
				@Html.DisplayFor(modelItem => item.DiasAntesEvento)
			</td>
			<td>
				@(item.Duracao.ToString())
			</td>
			<td>
				@if (item.PodeDomingo) { @(HttpUtility.HtmlDecode(Html.DisplayNameFor(m => m.PodeDomingo).ToHtmlString()) + "| ") }
				@if (item.PodeSegunda) { @(HttpUtility.HtmlDecode(Html.DisplayNameFor(m => m.PodeSegunda).ToHtmlString()) + "| ") }
				@if (item.PodeTerca) { @(HttpUtility.HtmlDecode(Html.DisplayNameFor(m => m.PodeTerca).ToHtmlString()) + "| ") }
				@if (item.PodeQuarta) { @(HttpUtility.HtmlDecode(Html.DisplayNameFor(m => m.PodeQuarta).ToHtmlString()) + "| ") }
				@if (item.PodeQuinta) { @(HttpUtility.HtmlDecode(Html.DisplayNameFor(m => m.PodeQuinta).ToHtmlString()) + "| ") }
				@if (item.PodeSexta) { @(HttpUtility.HtmlDecode(Html.DisplayNameFor(m => m.PodeSexta).ToHtmlString()) + "| ") }
				@if (item.PodeSabado) { @(HttpUtility.HtmlDecode(Html.DisplayNameFor(m => m.PodeSabado).ToHtmlString()) + "| ") }
			</td>
			<td>
				@Html.ActionLink("Editar", "Edit", new { id = item.Id }, new { @class = "PopUpActionLinks", @title = "Editar" }) |
				@Html.ActionLink("Excluir", "Delete", new { id = item.Id }, new { @class = "PopUpActionLinks", @title = "Excluir" })
			</td>
		</tr>
	}

</table>
